# Boxbound revision 4 验证

本地诊断证据，工作区有未提交修改，不是正式发布基准。构建、源码及截图指纹见 `validation-v4.json`。

- 普通门净宽 0.82 格；角色宽 0.72 格；箱子规则尺寸为 1 格。四方向窄门均阻止箱子出入，改为 1 格后允许运输。第 7、8 关需要运输的内层出口/入口保留 1 格。
- 墙顶凸点使用比墙顶略深的浅灰绿，已核对桌面与手机截图。
- 真实键盘事件验证：按住方向再按 J、先按 J 再按方向、单按 J 原地跳、释放方向，以及原有跳上盒顶再下钻。
- R 留在当前小世界，恢复当前空间及其内层；其他关卡和外层布局保留。已完成关卡可重新完成并庆祝，撤销可恢复重玩前状态。
- 五个旧存档迁移到 rulesRevision 4，保留进度、已运出的箱子和路径；与新增窄门门框冲突的旧箱子移到最近可容纳位置。

检查结果：

- `npm run typecheck` 通过；最终 Boxbound 独立 TypeScript 检查与 ESLint 通过。
- 规则、存档及预览相关测试 46 / 46 通过，包括十关完整解法和十关重玩解法。
- `GAME_FILTER=boxbound GAME_BUILD_TIMEOUT_MS=180000 npm run build` 通过。
- `node scripts/verify-boxbound.mjs`：8 / 8 场景、187 条浏览器断言通过，WebGPU 错误为 0；使用固定动画时钟与实际渲染帧。所有截图均绑定同一个最终 bundle。

全库执行 `npm test -- --test-concurrency=2`，共 842 项：803 通过、8 失败、11 超时取消、20 跳过。失败位于未修改的其他游戏模块：led-sudoku 的 flags 2/11/14 变体（各含 LED 开/关，共 6 项）、HYMUGEN 指纹与 MUGEN 虚拟列表断言；11 个本地 Petra 案例超时。没有修改或跳过这些用例，也没有放宽测试阈值。并行进行的 led-sudoku 修改保持原样。
