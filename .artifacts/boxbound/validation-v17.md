# Boxbound v17 验证

角色与箱子复用原有模型实体进出，按真实父子空间比例缩放。运输箱子不再同时出现于盒内 LOD/外景预览，缩放完成后原模型直接落在目标位置。LOD 与正式墙体共用墙身、浅色墙顶材质，箱子与铺装也使用相同颜色规则。

- 156 项 Boxbound 局部测试通过，包括真实目标位置、不淡出、双向缩放、41 关求解及存档回归。
- 10 个 Chrome/WebGPU 定向画面、391 条断言通过。实际核对角色和箱子动画前、中、后的实体 ID；检查运输期间单份绘制、角色随后出盒不再复制外部箱子、墙顶与墙身 RGBA 完全一致；覆盖手机、撤销、自引用和多层返回。
- 14 项资源检查通过：同房间 96 次移动复用实体；反复进出/运输后 GPU、CPU 对象数量保持稳定；四次销毁回到同一基线。
- Boxbound TypeScript、ESLint、单游戏构建通过。目视检查主要动画帧。
- 本地诊断绑定源码、构建和截图哈希；未改引擎、未跑全库测试、未操作当前用户游戏存档。
