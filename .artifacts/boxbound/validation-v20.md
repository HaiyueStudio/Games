# Boxbound v20 局部验证

- Enter 18：出生点走到南侧边界并向外退出，正确记录导航路径中隐含的物理跨层。
- 原角色实体与首帧屏幕位置连续，缩放到外层盒子的南侧出口；撤销沿相同容器反向播放。
- 验证退出后重新推动/进入、堵塞出口、普通走格和返回章节不误报跨层。
- 165 项局部测试通过；TypeScript、ESLint、单游戏构建通过。
- 4 组浏览器视图、159 项断言通过，WebGPU 未分类错误为 0；截图已核对。
- 临时浏览器使用内存存档；未改引擎、未跑全库测试；本次未重跑资源负载。
